---
phase: 04-tekton-pipeline-mlops
task: makefile-resume
total_tasks: 4
status: in_progress
last_updated: 2026-03-09T16:44:14.249Z
---

<current_state>
Building a Makefile-driven workflow so `make resume GENS=2` handles everything from cold start:
cluster, Tekton Loop deploy, PVC, backup restore, mGBA image, resume from best checkpoint.
~90% working. Missing: ROM/Lua/savestate PVC setup and dashboard.
</current_state>

<completed_work>

- Makefile created with targets: ensure-cluster, deploy-tekton(-force), deploy-pipeline, ensure-pvc, ensure-image, build-image, load-image, resume, train, status, save-data, load-data, delete-cluster, clean-runs
- scripts/resume-training.sh: auto-finds best island by fitness, copies checkpoint, launches PipelineRun
- PVC backup/restore working (save-data/load-data with auto-load on new PVC)
- Fixed: SHELL := /bin/bash, namespace creation, KO_DOCKER_REPO=kind.local, Pipeline CRD preserve-unknown-fields
- Fixed: feat/pipeline-iteration branch had orphaned loop commits -- rebased (git rebase --onto 26b1e999e bf12c8d1f feat/pipeline-iteration)
- Tested end-to-end: cluster create, Tekton deploy, PVC restore, checkpoint find, PipelineRun launch all work
</completed_work>

<remaining_work>

- save-data must also backup lua/, roms/, savestates/ (currently only output/)
- Dashboard code not in repo -- was at localhost:8081 with dark theme + grid VNC view. Needs locating or rebuilding
- Wire dashboard startup into ensure-infra
- Full clean-room test after ROM/savestate fix
</remaining_work>

<decisions_made>

- Ephemeral busybox pods for all PVC operations (portable, no host mount needed)
- PipelineRun timeout "0" (1h default caused premature kills)
- Backup at data/pvc-backup.tar.gz (44MB)
- Separate deploy-tekton (idempotent) vs deploy-tekton-force
- Pipeline CRD and PipelineRun CRD both need x-kubernetes-preserve-unknown-fields patches
</decisions_made>

<blockers>
- Dashboard code location unknown
- ROM/Lua/savestates not in save-data backup yet
</blockers>

<context>
Kind cluster "saiyan" is running with Tekton Loop deployed. A PipelineRun was launched but needs
ROM/Lua on PVC. Tekton pipeline repo feat/pipeline-iteration branch has all loop commits (rebased).
</context>

<next_action>
Fix save-data to backup full PVC (lua/, roms/, savestates/), locate dashboard code, add to Makefile.
</next_action>
