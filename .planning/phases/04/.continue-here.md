---
phase: 04-tekton-pipeline-mlops
task: makefile-resume
total_tasks: 4
status: complete
last_updated: 2026-03-09T16:55:00.000Z
---

<current_state>
Makefile-driven workflow is complete. `make resume GENS=2` handles everything from cold start:
cluster, Tekton Loop deploy, PVC creation, backup restore, ROM/Lua/savestate seeding,
mGBA image loading, dashboard startup, and resume from best checkpoint.
</current_state>

<completed_work>

- Makefile created with targets: ensure-cluster, deploy-tekton(-force), deploy-pipeline, ensure-pvc, seed-pvc, ensure-image, build-image, load-image, resume, train, status, save-data, load-data, delete-cluster, dashboard, stop-dashboard, clean-runs
- scripts/resume-training.sh: auto-finds best island by fitness, copies checkpoint, launches PipelineRun
- PVC backup/restore working (save-data/load-data) — now backs up entire PVC (lua, roms, savestates, output)
- seed-pvc: copies ROM (as rom.gba), Lua scripts, and savestates from local repo to PVC
- dashboard: starts training_dashboard.py on port 8081, wired into ensure-infra
- Fixed: kubectl cp nesting (copy to /workspace/ not /workspace/lua/)
- Fixed: SHELL := /bin/bash, namespace creation, KO_DOCKER_REPO=kind.local, Pipeline CRD preserve-unknown-fields
- Fixed: feat/pipeline-iteration branch had orphaned loop commits -- rebased
- Tested end-to-end: cluster create, Tekton deploy, PVC restore+seed, checkpoint find, PipelineRun launch, dashboard all work
</completed_work>

<remaining_work>
None — all items from the original task list are complete.
</remaining_work>

<decisions_made>

- Ephemeral busybox pods for all PVC operations (portable, no host mount needed)
- PipelineRun timeout "0" (1h default caused premature kills)
- Backup at data/pvc-backup.tar.gz — full PVC backup (not just output/)
- Separate deploy-tekton (idempotent) vs deploy-tekton-force
- Pipeline CRD and PipelineRun CRD both need x-kubernetes-preserve-unknown-fields patches
- seed-pvc separated from load-data: static assets (ROM/Lua/savestates) vs training data (checkpoints/metrics)
- Dashboard at port 8081, PID tracked in .dashboard.pid, auto-started by ensure-infra
</decisions_made>
