---
phase: 03-containerization
task: mgba-migration
total_tasks: 3
status: in_progress
last_updated: "2026-03-06T14:51:29.852Z"
---

<current_state>
mGBA port code is 100% complete. Docker image builds successfully (multi-stage, mGBA 0.10.5 from source with Qt frontend). However, mgba-qt 0.10.5 does NOT have the --script CLI flag. Need to rebuild from git master branch where --script was merged (confirmed by mGBA maintainer endrift in Jan 2025, GitHub issue #2981).
</current_state>

<completed_work>

Task 1: New Dockerfile for mGBA - DONE
- Multi-stage build: builder compiles mGBA 0.10.5 from source (cmake -DBUILD_QT=ON -DBUILD_SDL=OFF -DUSE_LIBZIP=OFF)
- Runtime stage copies /opt/mgba, adds Xvfb+x11vnc+fluxbox+noVNC+s6-overlay
- Fixed: libzip-dev cmake config broken on bookworm/arm64, disabled with -DUSE_LIBZIP=OFF
- s6 service directory renamed bizhawk/ -> mgba/
- docker-compose.yml and validate-speed.sh updated
- Image builds successfully and mgba-qt binary runs

Task 2: Port Lua scripts to mGBA API - DONE (all 11 files)
- memory_map.lua: memory.read_u8/u16_le/s16_le -> emu:read8/read16 + two's complement for signed
- controller.lua: joypad.set({A=true}) -> emu:setKeys(bitmask) with KEY_MAP table
- savestate_helper.lua: savestate.save/load -> emu:saveStateFile/loadStateFile
- main.lua: emu.frameadvance -> emu:runFrame, version bumped to v0.2.0
- training/loop.lua: All emulator calls updated
- training/checkpoint.lua: console.log -> console:log with userdata check
- game/inputs.lua: Comment updates (memory_map handles API)
- memory_reader.lua: Removed gui.* overlay, uses console:log
- controller_test.lua: Full rewrite with bitmask KEY table
- vis/network_display.lua, hud.lua, species_timeline.lua: Stubbed (mGBA lacks gui.* overlay)

Task 3: Update Tekton tasks - DONE
- run-training.yaml, evaluate-champion.yaml, evaluate-genome.yaml: image->saiyan-trainer/mgba:latest, command->mgba-qt --script
- pipeline.yaml: comment update
- All tasks add LIBGL_ALWAYS_SOFTWARE=1 and GALLIUM_DRIVER=llvmpipe

</completed_work>

<remaining_work>

- CRITICAL: Change Dockerfile ARG MGBA_VERSION from 0.10.5 to master (or find the exact commit/tag that includes --script for Qt). The --script flag is NOT in 0.10.5 release. Rebuild image.
- After rebuild: verify `docker run --rm --entrypoint="" saiyan-trainer/mgba:latest mgba-qt --help` shows --script
- Test container startup with a ROM: verify s6 services start, mGBA renders to Xvfb, noVNC accessible
- Test Lua script loading: verify mgba-qt --script actually loads and runs our training script
- Amend or create new commit with the Dockerfile MGBA_VERSION fix

</remaining_work>

<decisions_made>

- Build mGBA from source instead of Debian package (Debian only has mgba-sdl, no mgba-qt for arm64)
- Use Qt frontend for --script CLI flag (SDL frontend lacks it, confirmed via GitHub issue #2981)
- Multi-stage Docker build to keep image size manageable
- Disabled libzip (broken cmake config on bookworm/arm64, optional for mGBA)
- Visualization files stubbed to no-ops (mGBA lacks BizHawk gui.* overlay API)
- Controller uses local KEY_MAP bitmask table with bitwise OR
- Signed reads use manual two's complement

</decisions_made>

<blockers>

- mgba-qt 0.10.5 does NOT have --script flag. Must build from master branch. Risk: master may have build issues or API changes vs stable release.
- mGBA Lua API surface needs runtime validation once --script works. Documentation is sparse.

</blockers>

<context>
28 files changed across the codebase. NEAT algorithm (8 modules in lua/neat/) untouched since it's pure Lua. The Docker image builds and runs but needs the MGBA_VERSION changed to master for --script support. This is a one-line Dockerfile change + rebuild.

Two commits on main:
- 4b36931: wip: port from BizHawk to mGBA -- all code changes complete
- ab2dbfa: fix: disable libzip in mGBA build (broken cmake on bookworm/arm64)
</context>

<next_action>
1. Edit docker/Dockerfile line 30: change `ARG MGBA_VERSION=0.10.5` to `ARG MGBA_VERSION=master`
2. Rebuild: `cd docker && docker build -t saiyan-trainer/mgba:latest .`
3. Verify: `docker run --rm --entrypoint="" saiyan-trainer/mgba:latest mgba-qt --help | grep script`
4. If --script present: test with ROM, then commit
5. If master has build issues: try specific recent tags or commits after 0.10.5
</next_action>
