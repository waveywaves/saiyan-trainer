---
phase: 03-containerization
task: mgba-migration
total_tasks: 3
status: in_progress
last_updated: "2026-03-06T12:52:30.793Z"
---

<current_state>
Pivoting from BizHawk to mGBA emulator. BizHawk+Mono cannot run on Apple Silicon (ARM64) under any Docker emulation (QEMU or Rosetta). Mono JIT trampoline crashes at tramp-amd64.c:607, interpreter fails on Silk.NET OpenGL P/Invoke. Decision made to switch to mGBA which runs natively on ARM64/macOS.

Docker image partially modified during debugging but needs full rewrite for mGBA. Lua scripts not yet ported.
</current_state>

<completed_work>

This session (2026-03-06):
- Verified BizHawk+Mono incompatible with Apple Silicon (tested QEMU + Rosetta)
- Fixed Debian Bookworm mono-complete: use Mono project APT repo (stable-buster, version 6.12) instead of broken Debian 6.8
- Fixed s6-overlay v3 env var issue: all run scripts need `#!/command/with-contenv bash` shebang
- Validated noVNC + x11vnc + Xvfb + fluxbox stack works correctly
- Added Mesa software rendering packages (libgl1-mesa-dri, libegl1-mesa, libgles2)
- Researched mGBA Lua scripting API: memory reads, joypad, frame advance, save states, callbacks
- Created detailed API mapping (BizHawk -> mGBA) saved in memory/mgba-port.md
- Cleaned up all stale Docker containers on user's machine

Previous sessions:
- All 4 phases code complete (72 files, 36 commits)
- Full Tekton pipeline, NEAT engine, containerization, observability all implemented
</completed_work>

<remaining_work>

Task 1: New Dockerfile for mGBA
- Rewrite docker/Dockerfile to use mGBA instead of BizHawk+Mono
- mGBA is native ARM64, much lighter (no Mono dependency)
- May use `mgba-sdl` from Debian repos or build from source with CMake BUILD_QT=OFF
- Update s6 service from bizhawk to mgba
- Keep noVNC + Xvfb + x11vnc + fluxbox stack (working)

Task 2: Port Lua scripts to mGBA API
- ~11 files need changes (emulator API calls)
- ~13 files are pure NEAT logic (no changes needed)
- Key API changes documented in memory/mgba-port.md:
  - memory.read_u16_le(addr, "System Bus") -> emu:read16(addr)
  - joypad.set({A=true}) -> emu:setKeys(bitmask with C.GBA_KEY.*)
  - emu.frameadvance() -> emu:runFrame()
  - savestate.save/load(path) -> emu:saveStateFile/loadStateFile(path)
  - gui.* drawing -> mGBA canvas API (needs research)
  - emu.registerafter(fn) -> callbacks:add("frame", fn)

Task 3: Update Tekton tasks and test on Kind
- Change container image refs in k8s/tekton/tasks/*.yaml
- Update BizHawk CLI commands to mGBA CLI
- Create Kind cluster, load image, run pipeline
- User wants to see Tekton pipeline working on Kind locally
</remaining_work>

<decisions_made>

- Pivot from BizHawk to mGBA because BizHawk+Mono cannot run on Apple Silicon (fundamental incompatibility with ARM64 translation)
- Use Mono project APT repo (stable-buster) instead of Debian's broken mono-complete 6.8 (relevant if ever targeting x86_64)
- s6-overlay v3 services must use `#!/command/with-contenv bash` for env var inheritance
- Mesa software rendering (LIBGL_ALWAYS_SOFTWARE=1, GALLIUM_DRIVER=llvmpipe) for headless OpenGL
- User targets Kind for local K8s development on Mac
</decisions_made>

<blockers>
- mGBA headless CLI invocation needs verification (exact flags for loading script + ROM)
- mGBA canvas/drawing API for visualization overlay needs research (different from BizHawk gui.*)
- mGBA signed integer reads: BizHawk has read_s16_le(), mGBA read16() is unsigned -- need manual sign conversion
</blockers>

<context>
The project has all code implemented across 4 phases but BizHawk cannot run on the user's Apple Silicon Mac. The mGBA migration preserves all NEAT algorithm code (pure Lua) and only changes the emulator interface layer. The Tekton pipeline structure, SeaweedFS storage, Prometheus+Grafana observability all remain unchanged -- only the container image and the Lua scripts that interact with the emulator need updating.

The user wants to see the full Tekton pipeline running on Kind locally. The fastest path is: new mGBA Dockerfile -> port Lua scripts -> Kind cluster -> apply pipeline -> trigger PipelineRun.

Detailed API mapping and file-by-file change list saved in memory/mgba-port.md.
</context>

<next_action>
Start with: Read memory/mgba-port.md for the full API mapping and file list. Then:
1. Rewrite docker/Dockerfile for mGBA (check if mgba-sdl is in Debian bookworm repos)
2. Port lua/memory_map.lua first (foundational -- all other reads depend on it)
3. Port lua/controller.lua (button bitmask conversion)
4. Port lua/training/loop.lua and lua/main.lua (frame advance, save states, entry point)
5. Build image, test on Kind
</next_action>
