---
phase: 02.1-on-screen-visualization-training-fixes
task: post-completion-improvements
total_tasks: ongoing
status: training_running
last_updated: "2026-03-08T09:01:56Z"
---

<current_state>
4 island training PipelineRuns running on Kind cluster with all fixes applied.
Training is working (fitness 65->115 in Gen 0->1) but plateaued at ~115 fitness / 20 P2 damage across all islands through Gen 9.
Dashboard has new "Islands" comparison page with overlaid charts and export button.
Phase 5 (Blog Post) added to roadmap but not yet planned.
All code committed and pushed (9 commits this session).
</current_state>

<completed_work>

This session (2026-03-08):

1. Diagnosed fitness=-1 bug: P2 HP address wrong, fitness floor killed gradient, lastDamageFrame init wrong
2. Fixed P2 HP address: 0x03004C30 -> 0x03002826 via struct stride analysis (P2 Ki - P1 Ki = 0xE8 stride)
3. Fixed fitness function: removed -1 floor, added survival (W=5.0) + diversity (W=1.0) signals
4. Fixed lastDamageFrame: 0 -> nil (stall penalty only after damage dealt)
5. Reduced W_TIMEOUT_WIN 200->50 to break character-switch local optimum
6. Tuned config: Population 40, TimeoutConstant 600, StaleSpecies 12, NodeMutation 0.35
7. Added P2 HP diagnostic logging (evaluate_batch.lua + loop.lua)
8. Fixed checkpoint isolation bug: trainer was ignoring CHECKPOINT_DIR env, all islands sharing one dir
9. Added "Islands" page to dashboard: overlaid fitness/damage/species charts, summary table, JSON export
10. Updated all planning docs (STATE.md, ROADMAP.md, PROJECT.md, continue-here)
11. Added Phase 5 (Blog Post) to roadmap

Commits pushed (9):
- fix: correct P2 HP address via struct stride analysis
- fix: remove fitness floor and add gradient signals
- fix: lastDamageFrame init and pass combo entropy to fitness
- feat: add P2 HP address diagnostic to batch training
- tune: faster training and more diversity pressure
- chore: update project state after training fixes
- docs: update planning docs with training fix results and add Phase 5
- fix: use CHECKPOINT_DIR for trainer output isolation
- feat: add island comparison page to training dashboard

</completed_work>

<remaining_work>

- 4 islands training (saiyan-island-{b6h7g,9htg9,xt2m9,x779d}), ~Gen 9 of 100, will run for ~4-5 more hours
- All islands plateaued at 115 fitness / 20 P2 damage -- structural limit with 4 inputs and no spatial info
- Phase 5 (Blog Post) needs planning via /gsd:plan-phase 5
- Dashboard may need further polish for blog screenshots
- Consider adding spatial inputs (dist_x, dist_y) to break the 20-damage plateau
- Island migration (genome exchange between islands) not implemented

</remaining_work>

<decisions_made>

- P2 HP at 0x03002826 via struct stride 0xE8 (confirmed: P2 HP changes during fights)
- round_state (0x03002826) was actually P2 HP -- "instant win" = P2 HP=0 = KO
- Fitness floor removed; 0.001 minimum separates evaluated from unevaluated genomes
- W_TIMEOUT_WIN 200->50 so offense signal (3.0/HP) dominates over char-switch strategy
- Survival + diversity fitness signals provide gradient even without P2 damage
- Stall penalty only applies when damage was dealt then stopped (lastDamageFrame > 0)
- output_dir derived from CHECKPOINT_DIR (strips /checkpoints) for proper island isolation
- Blog post added as Phase 5 (not written yet, needs training data + screenshots first)
- After validating a fix, immediately restore full running state (user preference noted)

</decisions_made>

<blockers>

- 20-damage plateau across all 4 islands: only 4 inputs (P1HP, P2HP, P1Ki, P1Power), no spatial info
- mGBA runs at real-time 60fps (no turbo mode), ~5 min/gen with 40 genomes * 600 frames
- Species collapse: 40->4-5 by Gen 4-5 despite diversity pressure tuning
- Combo entropy drops to 0: best genomes all converge on single button pattern

</blockers>

<context>
Training infrastructure is solid and running. The NEAT algorithm works, selection pressure is real, and fitness differentiation exists. The bottleneck is now the input space: with only HP/Ki/Power as inputs and no spatial info (position, distance, attack states), the network can't learn WHEN to attack relative to the opponent.

The 20-damage-per-fight ceiling is likely one lucky punch pattern that gets repeated. To break through, either:
1. Add spatial inputs (dist_x, dist_y from memory map, unverified but could try)
2. Add frame-based temporal inputs (e.g., attack state, animation frame)
3. Increase TimeoutConstant to give more time for combos

The dashboard at localhost:8081 shows the Islands page with per-island comparison charts. The JSON export button downloads data for the blog post.

4 PipelineRuns active:
- saiyan-island-b6h7g (island-1)
- saiyan-island-9htg9 (island-2)
- saiyan-island-xt2m9 (island-3)
- saiyan-island-x779d (island-4)

Check training: kubectl --context kind-saiyan get pipelinerun
Dashboard: python3 scripts/training_dashboard.py --port 8081 (already running)
</context>

<next_action>
1. Check training results: curl -s http://localhost:8081/api/island-stats | python3 -mjson.tool
2. If training complete: collect champion genomes from each island for blog post
3. Plan Phase 5 (Blog Post): /gsd:plan-phase 5
4. Consider adding spatial inputs to break the 20-damage plateau
</next_action>
